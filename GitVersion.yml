branches:
  feature:
    increment: Minor
    regex: ^(feature|feat)/.*$
  fix:
    increment: Patch
    regex: ^fix/.*$
  main:
    regex: ^main$
mode: semantic
next-version-bump-type: Minor
version: 0
